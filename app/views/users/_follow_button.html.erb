<% if !@is_own_user and @logged_in %>

  <div id='follow-button' class='follow-button'>
    <% if @is_following %>
      <a>Follow</a>
    <% else %>
      <a>Unfollow</a>
    <% end %>
  </div>

<% end %>
