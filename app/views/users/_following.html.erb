<div id='user-following-container'>
  <% @user_following.each do |f| %>
    <div class='user_summary'>
      <div id='photo'>
        <img src=<%= f.photo_link%>>
      </div>
      <div class='name'>
        <a> <%= link_to(f.fullname, {:action => 'show', :id => f.followed_id}) %> </a>
      </div>
    </div>
  <% end %>
</div>

